/** Defaults match infra/dev/docker-compose.yml so search works with zero config. */
export const DEFAULT_MEILI_HOST = "http://localhost:7700";
export const DEFAULT_MEILI_KEY = "dev_meili_master_key_change_me";

/** The one index we maintain for song discovery. */
export const SONG_INDEX = "songs";
